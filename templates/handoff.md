# HANDOFF — read this first

**Updated:** YYYY-MM-DD · **Branch:** `<branch>` · **Last commit:** see `git log -1`

<One sentence: what this project is and that the reader has no prior context.>
This file orients you in about two minutes. It does not replace the plan; it tells you which
parts of which documents are live and which are stale.

> **Regenerated by `/checkpoint`, never appended to.** It is a snapshot of *now*. If it disagrees
> with the plan, the plan is authoritative for *what to do* and this file is authoritative for
> *where things stand* — and the disagreement is a bug to fix, not to live with.

---

## 1. Read these, in this order

| # | File | Why |
|---|------|-----|
| 1 | **`<active plan>`** | The execution document. Note which section is current. |
| 2 | **`<defect / corrections log>`** | Read before touching any model. |
| 3 | `<traceability or spec doc>` | Requirement → work → evidence mapping. |
| 4 | `SESSION_REPORT.md` (tail) | What happened, session by session. |

Then: `git log --oneline -12` and run the health checks below.

---

## 2. Current state in one paragraph

<Where the project stands right now. No history, no narrative of how it got here — that is
SESSION_REPORT's job. What is settled, what is open, what is blocked on what.>

---

## 3. Verify the environment before doing anything

```bash
<command>   # expect: <exact expected output>
```

<Anything about data location, caches, or environment that will silently produce wrong results if
misunderstood.>

---

## 4. The N things most likely to trip you up

<3–7 items. These are the things that cost real time to rediscover — a result that must never be
quoted, a design that looks failed but is not, a capability that does not exist, a filter that is
load-bearing. Each one line, imperative, with the reason.>

---

## 5. What to do next

### Track A — <name>
<Enough design detail to start cold: what, why, the approach, the traps.>

### Track B — <name>
<Same.>

### Also open
<Backlog, one line each.>

---

## 6. Conventions that are not negotiable

<The rules a newcomer would otherwise violate. Assertion discipline, registration of outputs,
reproducibility standard, what gets updated in the same commit as code.>

---

## 7. Known-unverified — do not treat as fact

<Anything currently used as if true that has not been verified, and what would verify it.>
