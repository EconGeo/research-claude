# Stage 0 — RED RECORD

**Date:** 2026-09-08 · **Task:** 0.4 (`tests/run_fixture.sh` skeleton, mechanical tier)
**Purpose:** Stage 0's entire point is to prove every new pipeline gate FAILS before anything
makes it pass. This file records that failure as it was actually observed, with the harness's
complete stdout, so a later green can be checked against a real baseline instead of a claim.

## run_fixture.sh

Command run from the worktree root (`$W` = `$RC` =
`/Users/andrew.mueller/Academic/research-claude/.claude/worktrees/pipeline-repair`):

```
$ chmod +x tests/run_fixture.sh && tests/run_fixture.sh; echo "exit=$?"
```

Full observed output:

```
══ fixture copy: /var/folders/41/ntzmfcsx5ms7vbngfv__6z540000gr/T/tmp.tbEX5QRQTW
PASS [link]
FAIL [manuscript-declared] python3: can't open file '/Users/andrew.mueller/Academic/research-claude/.claude/worktrees/pipeline-repair/scripts/pipeline.py': [Errno 2] No such file or directory
FAIL [state-init] python3: can't open file '/Users/andrew.mueller/Academic/research-claude/.claude/worktrees/pipeline-repair/scripts/pipeline.py': [Errno 2] No such file or directory
FAIL [state-valid] python3: can't open file '/Users/andrew.mueller/Academic/research-claude/.claude/worktrees/pipeline-repair/scripts/pipeline.py': [Errno 2] No such file or directory
FAIL [registry-check] python3: can't open file '/Users/andrew.mueller/Academic/research-claude/.claude/worktrees/pipeline-repair/scripts/pipeline.py': [Errno 2] No such file or directory
PASS [pre-writer-red]
PASS [render]
PASS [prose-check]
✗ run_fixture: FAIL
exit=1
```

**Why this is red:** `scripts/pipeline.py` does not exist yet (it ships in Task 1.7). Every
check that shells out to it — `manuscript-declared`, `state-init`, `state-valid`,
`registry-check` — fails with the same "can't open file" error, exactly as the brief predicts.
`link`, `render`, and `prose-check` do not depend on `pipeline.py` and genuinely pass: `apply.sh
--project-dir "$T" --link` links the pipeline into the fixture copy, `quarto render
manuscript_fixture.qmd` renders cleanly against the fixture's real tracked data
(`tests/fixture-project/data/raw/panel.csv`, 401 lines), and `prose_number_check.py` finds no
violations in `manuscript_fixture.qmd`. `pre-writer-red` also reports PASS, but see the vacuous
reds section below — it is not evidence that anything about the writer gate works. Overall exit
code is `1` because four checks failed, matching the brief's expectation exactly.

## Observed vs expected (brief Step 2)

| Check | Expected | Observed | Match |
|---|---|---|---|
| `link` | PASS | PASS | yes |
| `manuscript-declared` | FAIL — can't open file `.../scripts/pipeline.py` | FAIL — same error | yes |
| `state-init` | FAIL — can't open file `.../scripts/pipeline.py` | FAIL — same error | yes |
| `state-valid` | FAIL — can't open file `.../scripts/pipeline.py` | FAIL — same error | yes |
| `registry-check` | FAIL — can't open file `.../scripts/pipeline.py` | FAIL — same error | yes |
| `pre-writer-red` | PASS (vacuously) | PASS | yes |
| `render` | PASS | PASS | yes |
| `prose-check` | PASS | PASS | yes |
| overall exit | 1 | 1 | yes |

No divergence from the brief was observed.

## Vacuous reds

- **`pre-writer-red`** (`expect_fail pre-writer-red python3 "$RC/scripts/pipeline.py" --root "$T"
  pre writer`) reports `PASS`, but this is a **fake green**, not a real one. `expect_fail` only
  asserts that the command exits non-zero — and it does, because `scripts/pipeline.py` does not
  exist at all, so `python3` fails with "can't open file" before any argument parsing, gate
  logic, or coder-critic-score check ever runs. The check has never exercised the actual
  predicate it is named for ("no coder-critic score yet blocks the writer stage"); it would
  report the identical `PASS` for any reason `pipeline.py` fails to run — a syntax error, a
  missing import, wrong permissions, anything.
- This green carries **no information** about whether the pre-writer gate logic is correct, or
  even exists. It must not be cited as evidence that gate is enforced.
- **Task 1.7**, which writes `scripts/pipeline.py`, must supply its own genuine red for this
  check before its later green can be trusted: run `pipeline.py --root "$T" pre writer` against
  a fixture state that has *not yet* accumulated a passing coder-critic score and confirm it
  fails for that reason (not "file not found"), then confirm it starts passing only once the
  score is present. Until that genuine red is recorded, a green `pre-writer-red` in this harness
  proves nothing beyond "the script exists and ran."
